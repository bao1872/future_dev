# future_dev

期货量化开发目录（TqSdk）。

## 说明

仓库初始化时仅包含基础骨架，主脚本待脱敏后单独提交。

## 环境

```bash
python -m pip install -U tqsdk pandas
```

快期账号密码通过环境变量注入，不写入代码：

```bash
export TQ_USER="你的快期账号"
export TQ_PASSWORD="你的快期密码"
```
